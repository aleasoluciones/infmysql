export TEST_MODE=1

export MYSQL_HOSTNAME=127.0.0.1
export MYSQL_PORT=3313
export MYSQL_USER=test_user
export MYSQL_PASSWORD=test_password
export MYSQL_DB=mysql

export MYSQL_DB_URI="mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@${MYSQL_HOSTNAME}:${MYSQL_PORT}/${MYSQL_DB}"
